# Gemini CLI Extension

The `packages/gemini-extension` folder contains a minimal extension manifest
with commands for checking, explaining, and initializing VibeSafe.

This is a starter integration. The CLI and core rule engine are the source of
truth for v0.1.
